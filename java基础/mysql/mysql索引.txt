0、什么是索引
	索引是对数据库表中一列或多列的值进行排序的一种数据结构。
	索引的目的是为了减少io开销。

1、什么是主键索引
	唯一标识数据库表中的某一行的一列或多列的组合。主键索引中的主键每个值都唯一。

2、什么是联合索引
	对多个字段同时建立的索引，每个字段都可以有自己的顺序。遵循最左前缀原则，从索引最左边的列开始检索。

3、什么是覆盖索引
	索引满足查询语句中字段与条件的数据。覆盖索引不需要访问表的数据，但是查询的时候需要列出所需的字段名。

4、什么是索引条件下推
	查询的字段包含在联合索引下，从左逐级减少查询范围。